<div class="text-center">

    <h1 class="display-2">

        Mediçoes do Tanque Selecionado

    </h1>

    
    <div class="card shadow border-0">

        <div class="card-body">

            <div class="table-responsive">

                <table class="table table-hover align-middle">
                    <thead class="table-light">
                        <tr>
                            <th scope="col">Id</th>
                            <th scope="col">Data</th>
                            <th scope="col">Altura</th>
                            <th scope="col">Volume</th>
                            <th scope="col"></th>
                        </tr>
                    </thead>
                    <tbody>

                        @foreach (var medicao in Model)
                        {
            
                            <tr>
                                <th scope="row">@medicao.Id</th>
                                <td>@medicao.DataMedicao</td>
                                <td>@medicao.Altura</td>
                                <td>@medicao.Volume</td>


                                <td>
                                    <a type="button" class="btn btn-secondary" asp-action="Editar" asp-route-id="@medicao.Id">Editar</a>
                                    <a type="button" class="btn btn-danger" asp-action="Apagar" asp-route-id="@medicao.Id">Apagar</a>
                                </td>

                    
                            </tr>

                        }


                    </tbody>
                </table>
                <div class="d-grid gap-2">
                    <a class="btn btn-primary" type="button" asp-controller="Medicoes" asp-action="Adicionar" asp-route-tanqueId="@ViewBag.TanqueId">Realizar medicao</a>
                    <a class="btn btn-secondary" type="button" asp-controller="Postos" asp-action="Index">Voltar</a>
                </div>
            </div>

        </div>
    </div>


    
</div>